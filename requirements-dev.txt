-r requirements.txt

# Testing
pytest>=8.3.0,<9.0.0
pytest-asyncio>=0.24.0,<1.0.0

# Health check
httpx>=0.27.0,<1.0.0
redis>=5.0.0,<6.0.0
# Note: we use subprocess for docker checks to avoid needing the full docker py lib for simple `docker ps`

# TensorRT (install via NVIDIA PyPI index on the target device)
# tensorrt>=10.0.0
# pycuda>=2024.1
# numpy>=1.26.0,<3.0.0
